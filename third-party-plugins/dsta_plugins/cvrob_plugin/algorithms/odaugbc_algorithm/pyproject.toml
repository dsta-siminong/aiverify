[project]
name = "odaugbc_algorithm"
version = "0.1.0"
description = "Object Detection Augmentation by Class Algorithm"
authors = [{ name = "Example Author" }]
readme = "README.md"
dependencies = ["aiverify-test-engine[all]"]
requires-python = ">=3.10,<3.12"
license = { file = "LICENSE" }
[tool.setuptools.packages.find]
include = ["odaugbc_algorithm"] 