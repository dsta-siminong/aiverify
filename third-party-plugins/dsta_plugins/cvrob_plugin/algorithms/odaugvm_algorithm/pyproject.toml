[project]
name = "odaugvm_algorithm"
version = "0.1.0"
description = "Object Detection Algorithm v Metric Algorithm"
authors = [{ name = "Example Author" }]
readme = "README.md"
dependencies = ["aiverify-test-engine[all]"]
requires-python = ">=3.10,<3.12"
license = { file = "LICENSE" }
[tool.setuptools.packages.find]
include = ["odaugvm_algorithm"] 