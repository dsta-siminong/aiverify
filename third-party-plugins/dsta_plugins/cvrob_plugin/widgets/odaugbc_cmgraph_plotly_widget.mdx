export const cid = "odaugbc_algorithm"
//export const ib_cid = "augbyclass_inputblock"


<div style={{ marginBottom: "1rem" }}>
  <div
    style={{
      textAlign: "center",
      fontWeight: "bold",
      fontSize: "2.2rem",
      color: "#b91c1c",
      textDecoration: "underline"
    }}
  >
    {props.properties.title}
  </div>
</div>

import {
  transformData,
  ClassLineChart,
  PopulationChart,
  StackedBarChart,
  ClassMetricsTable
} from './augbyclass-chart.jsx';

{props.getResults(cid)?(
  (() => {
    const args = props.getResults(cid).ALGORITHM_ARGUMENTS;

    return (
      <div style={{
        border: "1px solid #e5e7eb",
        borderRadius: "8px",
        padding: "1rem",
        background: "#fafafa",
        margin: "1rem 0"
      }}>
        <div style={{
          fontWeight: "bold",
          marginBottom: "0.75rem",
          fontSize: "1.05rem"
        }}>
          Algorithm Parameters
        </div>

        <table style={{ width: "100%", borderCollapse: "collapse" }}>
          <tbody>
            {Object.entries(args).map(([key, value]) => (
              <tr key={key}>
                <td style={{
                  fontWeight: "600",
                  padding: "0.35rem 0.5rem",
                  verticalAlign: "top",
                  width: "35%"
                }}>
                  {key.replace(/_/g, " ")}
                </td>
                <td style={{
                  padding: "0.35rem 0.5rem",
                  color: "#374151",
                  wordBreak: "break-all"
                }}>
                  {String(value)}
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    );
  })()
):(
  <div>No algorithm data</div>
)}

export const AugmentationClassPlots = ({ props }) => {
  const resultsData = props.getResults ? props.getResults(cid).results : null;

  if (!resultsData || resultsData.length === 0) {
    return <div>No results data available</div>;
  }

  return (
    <div style={{ display: "flex", flexDirection: "column", gap: "3rem" }}>
      {resultsData.map((augmentationItem, index) => {
        const { Augmentation, plot_paths } = augmentationItem;
        const result = props.getResults(cid);
        const class_names = result.class_names;
        const combined = transformData(
          augmentationItem.classification_report,
          augmentationItem.conf_matrix,
          Object.keys(augmentationItem.display_info),
          class_names
        );
        
        if (!plot_paths || !plot_paths.class_plot) {
          return (
            <div
              key={index}
              style={{
                padding: "1em",
                border: "1px dashed #ccc",
                borderRadius: "8px",
                textAlign: "center",
                color: "#777",
              }}
            >
              <h3>{Augmentation}</h3>
              <div>No image available in sample</div>
            </div>
          );
        }
        
        const classPlot = plot_paths.class_plot;
        const plotlyPath = plot_paths.plotly_image_path;

        return (
          <div
            key={index}
            style={{
              border: "1px solid #ccc",
              borderRadius: "10px",
              padding: "2rem",
            }}
          >
            {/* 🔴 Augmentation Title */}
            <h2
              style={{
                textAlign: "center",
                fontWeight: "bold",
                textDecoration: "underline",
                color: "red",
                marginBottom: "2rem",
              }}
            >
              {Augmentation}
            </h2>

            {/* 🔹 Per Class Section */}
            {Object.entries(classPlot).map(([className, paths], i) => {
              const [filler1, filler2, filler3, metricPath, confmPath, popPath] = paths;

              let metricUrl = null;
              let confmUrl = null;
              let popUrl = null;

              try {
                metricUrl = props.getArtifactURL(cid, metricPath);
                confmUrl = props.getArtifactURL(cid, confmPath);
                popUrl = props.getArtifactURL(cid, popPath);
              } catch (err) {
                console.error("Artifact URL error:", err);
              }

              return (
                <div
                  key={i}
                  style={{
                    marginBottom: "3rem",
                    textAlign: "center",
                  }}
                >
                  <h3 style={{ marginBottom: "1rem" }}>
                    Class {className}
                  </h3>

                  {/* CM Section */}
                  <div style={{ marginBottom: "1rem", fontStyle: "italic" }}>
                    Confusion Matrix values for Class {className}
                  </div>

                  {confmUrl && (
                    <div
                      style={{
                        width: "100%",
                        height: "500px",        // visible box height
                        overflow: "hidden",
                        position: "relative",
                        border: "1px solid #ccc",
                        margin: "0 auto 2rem auto",
                      }}
                    >
                      <iframe
                        src={confmUrl}
                        title={`confm-${className}`}
                        style={{
                          width: "1200px",        // original content width
                          height: "1000px",       // original content height
                          transform: "scale(0.5)", // shrink to fit
                          transformOrigin: "top left",
                          border: "none",
                        }}
                      />
                    </div>
                  )}

                  <ClassMetricsTable combined={combined} className={className} metrics={["TP", "FP", "FN"]} />

                </div>
              );
            })}

          </div>
        );
      })}
    </div>
  );
};


<AugmentationClassPlots props={props} />

